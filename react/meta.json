{
    "kind": "template",
    "name": "react",
    "extend": ["default"],
    "scripts": {
      "before_install": "npx create-react-app {{name}} --template typescript && cp -n -r ./{{name}}/. . && rm -r ./{{name}}",
      "after_install": ""
    }
  }
  